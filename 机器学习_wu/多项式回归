多项式回归

#### **逻辑回归：**

解决二元分配问题

逻辑回归的底层逻辑是：先用线性回归计算一个连续得分，再通过 Sigmoid 函数将这个得分压缩到 (0, 1) 区间，解释为"属于正类的概率"，最后根据概率阈值完成二元分配。

过程可以概括为三步：**线性加权 → 概率转换 → 分类决策**

* **线性加权（得分计算）**：对输入特征进行线性组合，得到实数范围内的得分 $z$：$$z = w^Tx + b$$其中 $w$ 为权重向量，$b$ 为偏置项，$x$ 为特征向量。
  
* **Sigmoid 函数（概率映射）**：通过 Sigmoid 函数将得分 $z$ 压缩到 $(0, 1)$ 区间，解释为样本属于正类的概率
  
* **对数几率（Logit 变换）**：对 Sigmoid 求反函数，逻辑回归本质上是在用线性模型拟合“对数几率”：$$\ln\left(\frac{P}{1-P}\right) = w^Tx + b$$
  

#### 二元分配决策规则

模型输出概率后，通过设定阈值完成最终的类别分配：

* **默认规则**：$P(y=1|x) \geq 0.5$ 判为正类，否则判为负类。
* **决策边界**：由 $w^Tx + b = 0$ 定义，带入后得到一个方程，通过大于0或小于0进而判为0或1；在特征空间中是一个**超平面**（二维为直线，三维为平面）。

#### 参数估计与损失函数

逻辑回归不使用线性回归的“最小二乘法”，而是采用**极大似然估计（MLE）**。

* **损失函数（二元交叉熵损失）**：
  
* 当真实 y=1 时，公式后半部分直接变 0，只看前半部分： −log( y^​ ) （衡量预测正类的概率）当真实 y=0 时，公式前半部分直接变 0，只看后半部分： −log(1− y​ )（衡量预测负类的概率）
  
* 注：使用交叉熵，因为如果强行套用线性的 MSE 公式，整个损失函数的图像会变成一个起伏不平、有无数个“小坑”（局部极小值）的复杂曲面。这会导致梯度下降算法迷失方向，找不到全局最优解；
  
* 使用交叉熵而非平方误差，是因为交叉熵是凸函数，能保证梯度下降收敛到全局最优。预测接近1，损失是最小的
  
* **优化算法**：由于没有解析解，通常使用**梯度下降法**迭代求解。其梯度形式与线性回归高度相似：$$\frac{\partial J}{\partial w_j} = \frac{1}{m} \sum_{i=1}^{m} (\hat{y}_i - y_i) x_j^{(i)}$$
  

#### 正则化与防过拟合

当特征较多或存在多重共线性时，逻辑回归容易过拟合，常用正则化手段：

* **L1 正则化（Lasso）**：加入权重绝对值之和，能产生稀疏解，自动进行**特征选择**。
* **L2 正则化（Ridge）**：加入权重平方和，能缩小权重值，提高模型泛化能力。
