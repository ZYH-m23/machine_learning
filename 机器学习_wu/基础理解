机器学习

### AGI：

具备接近人类**通用认知能力**的人工智能系统。简单来说，它不是只会做某一件事的 AI，而是能像人一样跨领域学习、推理、规划，并独立解决各种陌生问题。

学习算法分类：

**监督学习（supervised learning)**

模型拿到的训练数据**都有标准答案（标签）**，像学生通过不断纠错，最终对于没有标签，能准确判断

分类：回归，分类

**无监督学习**

模型拿到的数据**没有标签**，它需要自己去发现数据中的规律和结构。

分类：聚类

**强化学习**

智能体在环境中**通过不断尝试来学习**，做对了给奖励，做错了给惩罚，目标是最大化长期累积奖励。
