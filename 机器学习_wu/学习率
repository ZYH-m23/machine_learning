
如何选择合适的学习率

学习率（Learning Rate, $\eta$）是梯度下降算法中最关键的超参数之一。选择合适的学习率，直接关系到模型能否收敛以及收敛的速度。

#### 一、 学习率对训练的影响

学习率的选择本质上是在**收敛速度**和**稳定性**之间寻找平衡：

* **学习率过大**：
  * **现象**：损失函数（Loss）在最优解附近剧烈震荡，甚至不断上升（发散），最终可能导致 Loss 变为 NaN。
  * **原因**：步长太大，导致模型“一脚踩空”，直接跨过了最低点，甚至跳到了对面更高的山坡上。
* **学习率过小**：
  * **现象**：损失函数下降极其缓慢，需要极多的迭代次数才能接近最优解。
  * **原因**：步长太小，模型像蜗牛一样挪动，不仅浪费时间，还容易陷入局部最优解（Local Minima）而无法自拔。
* **学习率适中**：
  * **现象**：损失函数平滑且稳定地下降，最终趋于水平，快速收敛到最优解附近。

#### 二、 初始学习率的经验值

在没有先验知识的情况下，可以通过以下经验值作为起点进行尝试：

* **通用试探范围**：通常从 `0.1`、`0.01`、`0.001` 等数量级开始试探。
* **不同优化器的默认值**：
  * **Adam / AdamW**：默认常设为 `3e-4` (0.0003) 或 `1e-3` (0.001)。
  * **SGD (带动量)**：通常比 Adam 高一个数量级，常用 `0.1` 或 `0.01`
  * **RMSProp**：适合 `0.001` 至 `0.1` 的范围。
* **不同任务场景**：
  * **从零训练普通网络**：常用 `0.01` 或 `0.001`。
  * **预训练模型微调（迁移学习）**：建议使用较小的学习率，如 `1e-4` 至 `1e-5`，以免破坏预训练好的权重。
  * **大模型（如 BERT 类）微调**：常用 `2e-5` 至 `5e-5`。

#### 三、 科学寻找最佳学习率的方法

除了手动试错，还有更科学的方法来定位最佳学习率：

**1. 学习率范围测试（LR Range Test）** 这是目前非常流行且高效的方法。具体操作是：

* 进行一次短暂的训练，让学习率从极小值（如 `1e-7`）线性或指数增长到极大值（如 `1e-1` 或 `1`）。
* 记录每个学习率对应的 Loss 值并绘制曲线。
* **选择策略**：选择 Loss 下降最快且最稳定的区间，或者 Loss 开始明显下降的拐点作为初始学习率。

**2. 网格搜索（Grid Search）** 在对数尺度区间（如 `1e-5` 到 `1e-1`）内，系统性地测试不同的学习率组合，通过验证集的表现来筛选最佳值。这种方法计算成本较高，但比较全面。

#### 四、 动态调整策略（学习率调度）

在训练过程中，固定的学习率往往不是最优的。通常采用“先大后小”的策略，即训练初期用较大的学习率快速接近最优解，后期用较小的学习率精细收敛。

* **预热（Warmup）**：
  * 在训练初期（前 5-10 个 Epoch 或总步数的 5%-10%），将学习率从 0 或极小值线性增加到目标学习率。
  * **作用**：防止模型在参数随机初始化阶段，因梯度不稳定而发生剧烈震荡或梯度爆炸。
* **衰减（Decay）**：
  * **阶跃衰减（Step Decay）**：每隔固定的 Epoch（如 10 或 30），将学习率乘以一个衰减系数（如 0.1）。
  * **余弦退火（Cosine Annealing）**：让学习率按照余弦函数的曲线平滑下降。这种方式有助于模型在训练后期跳出局部最优，是目前非常主流的策略。
  * **指数衰减（Exponential Decay）**：每个 Epoch 将学习率乘以一个固定的 Gamma 值（如 0.95），衰减平滑。

#### 五、 其他关键影响因素

* **批量大小（Batch Size）适配**：
  * 遵循**线性缩放原则**：当 Batch Size 增大 $k$ 倍时，学习率通常也应相应增大 $k$ 倍（或先尝试增大 1.5 倍）。
  * 小 Batch Size（如 8-16）通常需要配合较小的学习率（如 `5e-5` 至 `1e-4`）。
* **分层学习率（差异学习）**：
  * 在微调预训练模型时，可以对网络的不同层设置不同的学习率。例如，底层特征提取层使用较小的学习率（如新加层的 1/10），以保留预训练的通用特征；高层或新加的分类层使用较大的学习率，以快速适应新任务。

#### 六、 异常排查指南

在训练过程中，可以通过观察 Loss 曲线来动态调整学习率：

| 训练现象 | 可能原因 | 调整建议 |
| --- | --- | --- |
| **Loss 几乎不变** | 学习率过小 | 尝试将学习率调大 3 至 10 倍 |
| **Loss 剧烈震荡** | 学习率过大 | 尝试将学习率调小 3 至 10 倍 |
| **Loss 爆炸 (NaN)** | 学习率过大导致梯度溢出 | 大幅降低学习率，并加入梯度裁剪（Gradient Clipping） |
| **Loss 下降但效果变差** | 破坏了预训练权重 | 降低学习率（常见于微调场景） |

**注意：梯度下降的一个重要提示是 如果学习率足够小，成本函数应该在每次迭代中都减少**

通常找错方法：把学习了设置成很小的数，判断是否会导致成本在迭代中减少

### 方法顺序与使用场景

在实际的机器学习项目（如 SVM 核技巧或神经网络训练）中，调参并不是盲目试错，而是有一套科学的先后顺序。以下是推荐的实操流程：

#### 第一阶段：确定基准（初始化）

**1. 特征缩放（预处理）**

* **顺序**：**第一步**。在模型训练前必须完成。
* **方法**：优先使用 **Z-score 标准化**。
* **场景**：几乎所有依赖梯度下降的模型（线性回归、SVM、神经网络）。
* **目的**：消除量纲影响，让损失函数等高线变圆，为后续梯度下降打好基础。

**2. 设定初始学习率**

* **顺序**：**第二步**。在开始训练前设定。
* **方法**：根据优化器选择经验值。
  * **Adam**：默认 `1e-3` 或 `3e-4`。
  * **SGD**：默认 `0.1` 或 `0.01`。
* **场景**：所有梯度下降算法。

#### 第二阶段：寻找最佳范围（诊断）

**3. 学习率范围测试（LR Range Test）**

* **顺序**：**第三步**。在正式长时间训练前进行。
* **方法**：让学习率从极小值（`1e-7`）逐渐增加到极大值（`1`），观察 Loss 曲线。
* **场景**：当你不知道具体任务该用多大的学习率时。
* **目的**：快速定位 Loss 下降最快且稳定的区间，避免盲目网格搜索。

**4. 观察学习曲线（收敛判断）**

* **顺序**：**第四步**。在初步训练时观察。
* **方法**：绘制 Loss 随迭代次数变化的曲线。
* **场景**：判断当前学习率是否合适。
  * **震荡/发散** $\rightarrow$ 学习率过大，需调小。
  * **下降极慢** $\rightarrow$ 学习率过小，需调大。

#### 第三阶段：精细化训练（优化）

**5. 动态调整策略（学习率调度）**

* **顺序**：**第五步**。在确定了合适的初始学习率后，应用到正式训练中。
* **方法**：
  * **预热（Warmup）**：训练前几个 Epoch 线性增加学习率（防止初期梯度不稳定）。
  * **衰减（Decay）**：训练后期使用余弦退火或阶跃衰减，让学习率逐渐变小。
* **场景**：追求更高精度的模型，或训练深层神经网络、大模型微调。

**6. 批量大小（Batch Size）适配**

* **顺序**：**第六步**。如果调整了 Batch Size，需同步调整学习率。
* **方法**：遵循线性缩放原则（Batch Size 增大 $k$ 倍，学习率也相应增大）。
* **场景**：为了加速训练或受限于显存大小时。
