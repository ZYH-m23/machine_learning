线性回归

模型输出的数字可能是无限的

函数f被称为模型，x被称为输入特征，输出是预测值y-hat

核心：**代价函数**，衡量模型是否表现更好

**cost function:**

**(y预测-y)的平方的总和/（1/2m)**

### 梯度下降

一种算法，用于最小化代价函数

找最陡下降的地方，依次进行，直至到达最低点

![53b1331943cd418aafd42ebf00dace51](file:///C:/Users/38039/Pictures/Typedown/53b13319-43cd-418a-afd4-2ebf00dace51.png?msec=1789479954955)

一直更新w,b，直到算法收敛；两者更新完后，才开始赋值和下一轮更新

只要学习率合适，总是可以收敛到最小值

批量梯度下降：

在每一次更新模型参数时，都会遍历并使用**全部的训练样本**来计算梯度，然后沿着梯度的反方向来调整参数。

### 

进而通过向量实现其公式与运算的简便化

#### 特征缩放（Feature Scaling）

特征缩放是加速梯度下降收敛的关键预处理步骤，主要包含以下两种方法：

**1. 均值归一化（Mean Normalization）**

* **公式**：$x_j^{(i)} = \frac{x_j^{(i)} - \mu_j}{s_j}$
* **逻辑**：减去均值 $\mu_j$ 实现中心化，除以缩放因子 $s_j$（通常为最大值减最小值）实现缩放。
* **特点**：将数据严格限制在特定区间（如 -1 到 1），提供有正有负的特征值，使搜索路径更灵活。

**2. Z-score 标准化（Z-score Standardization）**

* **公式**：$z = \frac{x - \mu}{\sigma}$
* **逻辑**：减去均值 $\mu$ 实现中心化，除以标准差 $\sigma$ 实现缩放。
* **特点**：将数据转化为均值为 0、标准差 1 的标准分布。数值范围不固定，但对异常值（离群点）的鲁棒性更强，是机器学习中最常用的方法。

**3. 核心作用**

* **消除量纲影响**：防止数值范围大的特征主导模型，确保各特征权重公平。
* **加速收敛**：使损失函数的等高线图从狭长的椭圆形变为接近圆形，让梯度下降沿更直接的路径快速找到最优解。

#### 梯度下降收敛判断

**1. 正常收敛的判断标准**

* **绘制学习曲线**：横轴为迭代次数，纵轴为损失函数值。正常收敛表现为曲线平滑下降，最终趋于水平（更常用）
* **自动收敛测试**：设定极小阈值 $\epsilon$（如 0.001）。当相邻两次迭代的损失函数下降幅度小于 $\epsilon$ 时，判定为收敛。
* **梯度范数接近零**：当梯度的模长（L2范数）小于预设阈值时，说明参数已趋于稳定。

**2. 异常情况分析**

* **震荡或发散**：损失值忽大忽小或持续上升。原因通常是**学习率过大**，导致模型在最优解附近跳跃甚至越跳越远。
* **下降极其缓慢**：损失值下降斜率非常平缓。原因通常是**学习率过小**，导致需要极多的迭代次数，浪费计算资源。

#### 三、 实践注意事项

* **适用模型**：特征缩放对依赖梯度下降的模型（如线性回归、逻辑回归、SVM、神经网络）至关重要。
  
* **防止数据泄露**：必须**仅使用训练集**计算均值、标准差等统计量，然后用这组固定的参数去转换测试集和未来新数据。
  
* **学习率调优**：建议从较小的学习率（如 0.001）开始，观察损失曲线。若下降太慢，可逐步尝试增大（如 0.003, 0.01, 0.03），直到找到既迅速又平滑的下降曲线。
