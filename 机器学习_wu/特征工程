特征工程（Feature Engineering)

是机器学习中至关重要的一环，其本质是利用领域知识和现有数据，**创造出新的特征**，或是对原始数据进行转换和选择，以便更好地满足机器学习算法的需求。

特征工程的核心目标是提升信息密度、适配模型需求并降低计算成本。

### ️ 第一阶段：数据清洗与预处理（基础打底）

这是建模前的基础工作，目的是确保数据干净、一致且可用。

**1. 数据清洗（Data Cleaning）**

* **顺序**：**第一步**。
* **方法**：
  * **缺失值处理**：删除、填充（均值/中位数/众数）、插值法或预测法。
  * **异常值处理**：使用 Z-score、IQR（箱线图）检测并修正或删除极端值。
  * **去重与纠错**：去除重复记录，修正逻辑错误（如年龄为负数）。
* **场景**：所有机器学习项目，尤其是数据质量较差时。

**2. 数据转换与编码（Data Transformation & Encoding）**

* **顺序**：**第二步**。
* **方法**：
  * **类别编码**：独热编码（One-Hot，用于无序类别）、标签编码（Label，用于有序类别）、目标编码（Target，用于高基数类别）。
  * **时间序列处理**：从时间戳中拆解出年、月、日、星期、小时等周期性特征。
* **场景**：数据中包含文本、类别等非数值型特征时。

### 第二阶段：特征构建与转换（核心创造）

这是特征工程的灵魂，通过创造新特征或优化现有特征，提升模型的学习能力。

**1. 特征构建（Feature Construction）**

* **顺序**：**第三步**。
* **方法**：
  * **组合特征**：交叉特征（如“性别”+“地区”）、多项式特征（如 $x_1^2 + x_2 \times x_3$）。
  * **数学变换**：对数变换（处理长尾分布）、三角函数（处理角度周期性）。
  * **领域知识特征**：基于业务逻辑创造特征（如用“身高”和“体重”计算 BMI）。
* **场景**：原始特征表达能力不足，需要挖掘更深层信息时。

**2. 特征缩放（Feature Scaling）**

* **顺序**：**第四步**（在特征构建之后，模型训练之前）。
* **方法**：
  * **Z-score 标准化**：适用于大多数情况，尤其是数据有异常值时。
  * **归一化（Min-Max）**：将数据缩放到 [0, 1] 区间，适用于对数值范围有严格要求的场景（如图像处理）。
* **场景**：依赖梯度下降或距离计算的模型（如 SVM、KNN、神经网络）。

### 第三阶段：特征选择与降维（精简优化）

在特征变多后，需要筛选出最有价值的特征，去除冗余，防止过拟合。

**1. 特征选择（Feature Selection）**

* **顺序**：**第五步**。
* **方法**：
  * **过滤法（Filter）**：基于统计指标（如相关系数、卡方检验、方差阈值）独立筛选。
  * **包裹法（Wrapper）**：基于模型性能（如递归特征消除 RFE）进行子集搜索。
  * **嵌入法（Embedded）**：利用模型内部机制（如 Lasso/L1 正则化、树模型的特征重要性）自动选择。
* **场景**：特征维度高、存在多重共线性或训练速度慢时。

**2. 特征降维（Dimensionality Reduction）**

* **顺序**：**第六步**（可选，通常在特征选择之后或替代特征选择）。
* **方法**：
  * **线性方法**：主成分分析（PCA）、线性判别分析（LDA）。
  * **非线性方法**：t-SNE、UMAP。
* **场景**：需要大幅压缩数据维度、去除噪声或进行数据可视化时。

### 第四阶段：评估与迭代（闭环优化）

特征工程不是一次性的，而是一个“螺旋上升”的迭代过程。

* **顺序**：**贯穿始终**。
* **方法**：
  * **交叉验证**：评估不同特征组合对模型性能的影响。
  * **消融实验**：逐个移除特征，观察模型性能变化，判断特征的真实贡献。
* **场景**：模型性能遇到瓶颈，需要进一步优化时。

### 总结流程图

1. **数据清洗** $\rightarrow$ 处理缺失值、异常值
2. **数据编码** $\rightarrow$ 类别特征转数值
3. **特征构建** $\rightarrow$ 创造新特征、数学变换
4. **特征缩放** $\rightarrow$ Z-score 标准化或归一化
5. **特征选择** $\rightarrow$ 筛选重要特征（过滤/包裹/嵌入）
6. **特征降维** $\rightarrow$ PCA 等
7. **模型训练与评估** $\rightarrow$ 根据反馈迭代优化上述步骤

### ️ 关键注意事项

* **防止数据泄露**：所有的预处理和特征工程步骤（如缩放、PCA），**必须仅使用训练集**进行拟合（fit），然后用这组固定的参数去转换验证集和测试集。
  
* **先缩放后 PCA**：如果同时使用特征缩放和 PCA，必须先进行缩放。因为 PCA 基于方差，如果特征尺度不一致，数值大的特征会主导主成分。
